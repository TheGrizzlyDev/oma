load("@rules_python//python:defs.bzl", "py_binary")
load("@pypi//:requirements.bzl", "requirement")

exports_files(["rules.bzl"])

py_binary(
    name = "topology_cleanup",
    srcs = ["topology_cleanup.py"],
    deps = [
        requirement("shapely"),
    ],
)
